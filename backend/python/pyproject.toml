[project]
name = "noddle-py"
version = "0.1.0"
description = "Python bridge for Noddle graph prototyping"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
